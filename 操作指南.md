# 墨笺博客 · 操作指南

一份从「拿到这套模板」到「把博客发布上线」的完整说明。按顺序读一遍，照着做即可。

---

## 0. 这是什么

「墨笺 · Ink Letter」是一个**纯静态**的个人博客：

- 文章用 **Markdown** 写，放在 `posts/` 目录下；
- 浏览器读取 `posts/index.json` 索引，渲染首页列表与文章正文；
- 不需要数据库、不需要服务端、不需要构建步骤；
- `git push` 到 GitHub，由 **GitHub Actions + GitHub Pages** 自动发布。

设计上参考了 [matrix67](https://matrix67.com/blog/) 的学者式排版，与日式「留白（よはく）」的极简气韵：暖纸色底、墨色字、一枚朱砂印章作为唯一强调色，配合 Noto Serif SC / Fraunces / Newsreader 衬线字体。

> 关于你提到的本地路径：你在 Mac 上的 `/Users/thomaswang/Desktop/东西/个人blog` 就是这个仓库的本地副本。本指南里「仓库根目录」指的就是那个文件夹。另外 `Downloads/yohaku-blog-template` 我这边无法访问（在远端沙箱里），所以是按「留白」的字面含义 + matrix67 的气质来设计的；如果你想把 yohaku 模板里的某些具体元素融合进来，把那个文件夹里你最喜欢的截图或文件发我即可。

---

## 1. 目录结构

```
个人blog/
├── index.html              # 首页（文章列表）
├── post.html               # 文章正文页（根据 ?slug= 渲染）
├── archive.html            # 归档页（按年份分组 + 搜索）
├── about.html              # 关于页（静态，直接编辑 HTML）
├── assets/
│   ├── css/style.css       # 所有样式（改颜色、字号在这里）
│   ├── js/blog.js          # 所有逻辑 + 站点配置（改站名、评论在这里）
│   └── img/                # 文章图片放这里
├── posts/
│   ├── index.json          # 文章索引（manifest）
│   └── template.md         # 新文章模板
├── tools/
│   └── newpost.py          # 新建文章的小脚本（可选）
├── .github/workflows/
│   └── static.yml          # GitHub Pages 自动部署配置（已就绪）
└── 操作指南.md              # 你正在看的这份
```

---

## 2. 本地预览

因为文章是通过 `fetch` 读取的，**直接双击 html 打开（file://）会失效**，需要起一个本地服务器。任选一种：

```bash
# 方式 A：Python（Mac 自带）
cd /Users/thomaswang/Desktop/东西/个人blog
python3 -m http.server 8000

# 方式 B：Node
npx serve .
```

然后浏览器打开 <http://127.0.0.1:8000/>。改完文件刷新即可看效果。按 `Ctrl+C` 停止。

---

## 3. 写第一篇文章

### 方式一：用脚本（推荐）

```bash
cd /Users/thomaswang/Desktop/东西/个人blog
python3 tools/newpost.py "我的第一篇文章"
```

脚本会自动：

1. 复制 `posts/template.md` 为 `posts/my-first-post.md`（中文标题会退化为 `post-YYYY-MM-DD.md`，可手动改名）；
2. 填好今天的日期；
3. 在 `posts/index.json` 里追加一条索引记录。

然后打开生成的 `.md` 文件写正文即可。也可以指定 slug：

```bash
python3 tools/newpost.py "关于 Nim 游戏的若干变种" nim-variants
```

### 方式二：手动

1. 复制 `posts/template.md` 为 `posts/你的文件名.md`；
2. 改最上面的 frontmatter（见下一节）；
3. 在 `posts/index.json` 里加一条：

```json
[
  {
    "slug": "你的文件名",
    "title": "文章标题",
    "date": "2026-08-09",
    "tags": ["数学", "随笔"],
    "excerpt": "一句话摘要，显示在首页列表。"
  }
]
```

> 注意 `slug` 必须和 `.md` 文件名（去掉 `.md`）完全一致。索引数组顺序无所谓，会按日期自动倒序排列。

---

## 4. 文章 Frontmatter 字段

每篇 `.md` 顶部用 `---` 包住一段元信息：

```markdown
---
title: 文章标题
date: 2026-08-09
tags: [数学, 趣题]
excerpt: 一两句摘要。
---

正文从这里开始……
```

| 字段 | 必填 | 说明 |
|------|------|------|
| `title` | 是 | 文章标题 |
| `date` | 是 | `YYYY-MM-DD`，决定排序 |
| `tags` | 否 | `[标签1, 标签2]`，用于归档筛选 |
| `excerpt` | 否 | 摘要，显示在首页；不写则不显示 |

正文支持完整 Markdown：标题、粗斜体、列表、引用、链接、代码块（自动语法高亮）、图片、表格、分隔线。

### 图片

把图片放进 `assets/img/`，在文章里这样写：

```markdown
![图片说明](assets/img/photo.jpg)
```

### 代码块

````
```python
print("hello")
```
````

支持常见语言高亮（由 highlight.js 提供）。

---

## 5. 自定义外观

### 改站点名称 / 标语 / 作者

打开 `assets/js/blog.js`，最上面的 `CONFIG` 对象：

```js
const CONFIG = {
  siteName: "墨笺",
  siteTagline: "一个关于数学、代码与偶然想法的笔记本",
  author: "Thomas Wang",
  ...
};
```

页面顶部导航里的「墨笺 Ink Letter」字样是写在四个 HTML 文件（`index/post/archive/about.html`）的 `<header>` 里的，全局搜索替换即可。`about.html` 的内容也直接编辑 HTML。

### 改配色

打开 `assets/css/style.css`，最上面的 `:root` 就是颜色变量：

```css
--paper:  #faf7f0;   /* 纸色背景 */
--ink:    #1c1a17;   /* 正文墨色 */
--seal:   #a8322a;   /* 朱砂强调色（链接、印章、点） */
--gold:   #8c6a2f;   /* 次要金色 */
```

`[data-theme="dark"]` 下面是夜间模式的配色，改法相同。改一个变量，全站联动。

### 改字体

在四个 HTML 文件的 `<head>` 里有 Google Fonts 链接，以及 `style.css` 里的 `--serif / --display / --mono` 变量。换成你喜欢的即可（建议保留一款中文衬线如 Noto Serif SC）。

### 改首页那句引言

在 `index.html` 里搜「把想法 写下来」，改成你想要的话。

---

## 6. 开启评论（可选，基于 GitHub Discussions）

用 [giscus](https://giscus.app) 即可，评论存在你仓库的 Discussions 里。步骤：

1. 仓库 Settings → 勾选 Discussions；
2. 安装 [giscus app](https://github.com/apps/giscus) 并授权该仓库；
3. 去 <https://giscus.app> 填仓库名，生成 `data-repo-id` 和 `data-category-id`；
4. 把 `assets/js/blog.js` 里 `CONFIG.comments` 改成：

```js
comments: {
  enabled: true,
  repo: "你的用户名/你的仓库",
  repoId: "R_kgxxxxxx",
  category: "Announcements",
  categoryId: "DIC_xxxxxx",
  ...
}
```

文章底部就会自动出现评论区。

---

## 7. 发布到 GitHub Pages（重点）

### 7.1 在 GitHub 建仓库

在 <https://github.com/new> 新建一个仓库，比如叫 `blog`（公开或私有都行，Pages 在私有仓库上需要 Pro 账号，公开仓库免费）。

### 7.2 把本地仓库推上去

`/Users/thomaswang/Desktop/东西/个人blog` 已经是个 git 仓库了。第一次推送：

```bash
cd /Users/thomaswang/Desktop/东西/个人blog

# 看看当前状态
git status

# 如果还没设置远程
git remote add origin https://github.com/你的用户名/blog.git

# 提交所有文件
git add .
git commit -m "初始化墨笺博客"
git branch -M main
git push -u origin main
```

> 之后每次写完文章：`git add . && git commit -m "新文章：xxx" && git push`，等十几秒网站就更新了。

### 7.3 开启 Pages

仓库 **Settings → Pages**：

- **Source** 选 **GitHub Actions**（不是 Deploy from a branch）。

这样就会使用仓库里已有的 `.github/workflows/static.yml`。推送后到仓库的 **Actions** 标签页看构建进度，绿勾即成功。

### 7.4 访问地址

发布后地址通常是：

```
https://你的用户名.github.io/blog/
```

（仓库名叫 `blog` 就对应 `/blog/` 路径；想让根域名 `用户名.github.io` 直接是博客，就把仓库命名为 `你的用户名.github.io`。）

如果想用自定义域名，在 Pages 设置里填 Custom domain，并在仓库根目录加一个 `CNAME` 文件（里面写域名）即可。本站用的是**相对路径**，换域名不用改代码。

---

## 8. 日常写作流程（小结）

```bash
# 1. 起新文章
python3 tools/newpost.py "今天的一个想法"

# 2. 编辑 posts/xxx.md 写正文（用任意编辑器 / VS Code / Typora）

# 3. 本地预览
python3 -m http.server 8000   # 浏览器开 127.0.0.1:8000

# 4. 满意了，发布
git add .
git commit -m "新文章：今天的一个想法"
git push
```

完事。GitHub Actions 会在十几秒内把新文章发布上线。

---

## 9. 备选方案：Gmeek / Hugo

如果你之后觉得「每篇都要维护 `index.json`」略繁琐，可以迁移到下面任一方案，设计可以照搬这套 CSS。

### 9.1 Gmeek（用 GitHub Issues 当后台）

适合：**不想碰文件，直接在网页上写 Issue 发文章**。

- 仓库：<https://github.com/Meekdai/Gmeek>
- 原理：每篇 Issue = 一篇文章；新建/编辑 Issue 触发 Action 自动生成静态站并部署到 Pages。
- 优点：纯网页写作，Markdown 即时，自带评论（就是 Issue 评论）、标签、搜索、RSS。
- 迁移：新建一个仓库 → 按 Gmeek README 配置 `config.json`（里面可以自定义主题色、博客名）→ 在 Issues 里写文章。本套「墨笺」的配色（朱砂 `#a8322a` / 纸色 `#faf7f0`）可以直接填进 Gmeek 的主题配置。

### 9.2 Hugo / Eleventy（带构建步骤，SEO 更好）

适合：**追求最佳性能与 SEO、愿意学一点模板**。

- 文章用 Markdown 写，本地 `hugo server` 预览；
- `git push` 后由 GitHub Actions 跑 `hugo build`，把生成的 `public/` 部署到 Pages；
- 可以把本套 `style.css` 作为 Hugo 的自定义主题样式。
- 优点：预渲染 HTML，首屏无闪烁，SEO 友好，文章多时性能更好。

当前的纯静态方案是这三者里**最简单、零构建**的一个，适合起步；将来文章变多再迁移到 Hugo 也不晚，Markdown 文件可以原样搬过去。

---

## 10. 常见问题

**Q：本地打开是空白 / 报错「读取文章列表失败」？**
A：你用了 `file://` 打开。请用 `python3 -m http.server 8000` 起服务后再访问 `127.0.0.1:8000`。

**Q：首页显示「这里还没有文章」？**
A：正常。`posts/index.json` 是空的 `[]`。用 `python3 tools/newpost.py "标题"` 新建一篇就会出现在列表里。

**Q：发布后文章打不开（404）？**
A：检查 `posts/index.json` 里的 `slug` 是否和 `posts/` 下的 `.md` 文件名（去掉后缀）完全一致，注意大小写。

**Q：字体加载慢 / 想离线？**
A：中文字体（Noto Serif SC）通过 Google Fonts 按需分片加载。完全离线可把字体下载到 `assets/fonts/` 并改 `@font-face`，但文件较大，一般没必要。

**Q：想加 RSS / 站点统计？**
A：RSS 可加一个 `feed.xml`（手写或用脚本生成），统计可接 [Umami](https://umami.is) 或 Google Analytics，把脚本片段加到四个 HTML 的 `<head>`。

**Q：怎么彻底换一套配色？**
A：只改 `assets/css/style.css` 顶部 `:root` 和 `[data-theme="dark"]` 里的几个变量即可，全站联动。

---

## 11. 一句话总结

> 在 `posts/` 里写 Markdown，在 `posts/index.json` 里登记一行，`git push`。其余的，交给留白与朱砂。
