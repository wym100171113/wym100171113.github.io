# blog · wym — 发布与写作操作指南

一份从「写第一篇文章」到「发布上线」的完整操作说明。看完这份就够了。

---

## 0. 这是什么

「blog · wym」是一个**纯静态**的个人博客：

- 文章是 **Markdown 文件**，放在 `posts/` 目录下；
- 内置一套 **CMS 命令行工具**（`tools/blog.py`）：新建文章、本地预览、格式校验、重建索引、一键发布；
- **GitHub Actions 自动重建**：你只需要写 `.md` 文件并 `git push`，索引（`index.json`）、RSS（`feed.xml`）、站点地图（`sitemap.xml`）会自动重新生成，随后自动部署到 GitHub Pages；
- 不需要数据库、不需要服务器、不需要构建步骤，零成本。

排版灵感来自 **matrix67.com** 的学者式内容排版（中文日期、年度归档、分类标签），与 [yohaku-blog-template](https://www.stylekit.top/templates) 的极简纸感——白纸底色、墨色文字、一抹蓝色强调色。

> 本仓库对应线上地址：<https://wym100171113.github.io/>

---

## 1. 目录结构

```
wym100171113.github.io/
├── index.html              # 首页（动态列表，改帖子后自动更新，不用动）
├── archive.html           # 归档页（按年分组 + 搜索 + 标签筛选）
├── post.html              # 文章页（自动生成目录、渲染数学公式与代码高亮）
├── about.html             # 关于页（想改介绍，直接编辑这个 HTML）
├── 404.html               # 未找到页面
├── robots.txt / feed.xml / sitemap.xml   # 由 build 命令自动生成
├── assets/
│   ├── css/style.css      # 全部样式。配色、字体、间距都在这一个文件里
│   └── js/blog.js         # 前端逻辑：主题切换、列表渲染、目录生成
├── posts/
│   ├── *.md               # 你的文章（一篇 .md = 一篇文章）
│   └── index.json         # 文章索引 —— 自动生成，不要手改！
├── tools/blog.py          # CMS：new / serve / check / build / publish
└── .github/workflows/
    └── pages.yml          # 自动重建索引 + 自动部署（已就绪，别动）
```

---

## 2. 首次本地预览

```bash
cd /Users/thomaswang/Desktop/东西/个人blog/wym100171113.github.io
python3 tools/blog.py serve
```

浏览器打开 <http://127.0.0.1:8000/>，`Ctrl+C` 停止。

> **注意**：直接双击 HTML 文件（`file://`）会因浏览器安全限制而渲染失败，一定要用上面的方式起本地服务。

---

## 3. 写一篇文章（三选一）

### 方式 A：命令行（推荐，最顺手）

```bash
python3 tools/blog.py new "关于 Nim 游戏的若干变种" --tags 数学,趣题
```

会自动创建 `posts/关于-nim-游戏的若干变种.md`（中文标题回退为 `post-20260809.md`，可后续改名）：

- 已填好**今天的日期**与**标签**；
- 带有完整的 Markdown 模板；
- 若想指定文件名：`python3 tools/blog.py new "标题" --slug nim-variants`。

### 方式 B：手动新建

1. 在 `posts/` 下新建 `xxx.md`：
2. 照下面的模板写头部信息：

```markdown
---
title: 文章标题
date: 2026-08-09
tags: [数学, 趣题]
excerpt: 一两句摘要，显示在首页列表与 RSS 里。
---

正文从这里开始……

写支持：**粗体**、*斜体*、`行内代码`、表格、引用、列表、图片、代码块。

数学公式用 $$ 包裹（自动渲染 KaTeX）：

$$E = mc^2$$

`status: draft` 加上这行，文章就不会出现在线上（草稿模式）。
```

### 方式 C：在 GitHub 网页上直接编辑

1. 打开仓库 <https://github.com/wym100171113/wym100171113.github.io>
2. `posts/` → 右上角 **Add file** → 新建 `xxx.md`，粘贴上面的模板；
3. 填好内容后 Commit 到 `main`。

**三种方式殊途同归**：只要 `posts/` 下多了写好的 `.md` 文件，推送后网站自动更新。**你唯一需要维护的就是 Markdown 文件本身。**

---

## 4. Frontmatter 字段

| 字段 | 必填 | 说明 |
|------|------|------|
| `title` | 是 | 文章标题 |
| `date` | 是 | `YYYY-MM-DD`，决定排序与归档年份 |
| `tags` | 否 | `[标签1, 标签2]`。第一个标签会显示为文章分类 |
| `excerpt` | 否 | 摘要，显示在首页与 RSS；不写就不显示 |
| `status` | 否 | 写 `draft` 则此文章不发布（草稿） |

正文支持完整 Markdown 语法 + 两类特殊能力：

- **数学公式**：`$行内公式$` 与 `$$独立公式$$`（KaTeX 渲染，速度极快）
- **代码高亮**：```` ```python ```` 三反引号代码块自动高亮

### 图片

图片放到 `assets/img/` 目录，文中用相对路径引用：

```markdown
![示意图](assets/img/示例.png)
```

---

## 5. 发布上线（整个流程）

写完文章后，按顺序执行（或者只写 `.md` 文件，其余全自动）：

```bash
cd /Users/thomaswang/Desktop/东西/个人blog/wym100171113.github.io

# 1) 本地校验（title / date / 格式问题一眼看出）
python3 tools/blog.py check

# 2) 本地预览确认效果
python3 tools/blog.py serve

# 3) 重建索引 + RSS + 站点地图（保险起见手动跑一次，其实 Actions 也会自动跑）
python3 tools/blog.py build

# 4) 提交并推送 —— 一条命令完成
python3 tools/blog.py publish "新文章：Nim 游戏变种" --push
```

**发布之后**：GitHub Actions 流水线（`.github/workflows/pages.yml`）会自动：

1. 依据 `posts/*.md` 重新生成 `posts/index.json`（含阅读时长、标签、分类）；
2. 重算 `feed.xml` 与 `sitemap.xml`（有变化会自动提交一个 `chore:` 提交，重复触发无害）；
3. 部署到 GitHub Pages。

等约 1 分钟，访问 <https://wym100171113.github.io/> 即可看到新文章。也可以在仓库 **Actions** 标签页查看进度，绿勾即成功。

> 日常就两行命令：改完 `.md` → `python3 tools/blog.py publish "xxx" --push`。

---

## 6. 内容管理（CMS）命令速查

| 命令 | 作用 |
|------|------|
| `python3 tools/blog.py new "标题" [--tags 数学,算法] [--slug x]` | 新建文章（自动填日期，带模板） |
| `python3 tools/blog.py serve [--port 8000]` | 本地预览 |
| `python3 tools/blog.py check` | 检查所有文章的 frontmatter 合法性 |
| `python3 tools/blog.py build` | 重建 `index.json` / `feed.xml` / `sitemap.xml`（幂等） |
| `python3 tools/blog.py publish "说明" --push` | `git add + commit + push` 一步完成 |

---

## 7. 自定义站点

### 改站名 / 标语 / 记号

- **全站文案**：搜 HTML 里的「blog」「wym」，逐个替换；
- **首页大标题、随手记**：打开 `assets/js/blog.js` 顶部的 `CONFIG`，改 `thoughts` 数组即可；
- **站点地址 / 作者**：`tools/blog.py` 顶部 `SITE_URL` / `AUTHOR`；
- **关于页**：直接编辑 `about.html`。

### 改配色（改 4 个变量，全站联动）

打开 `assets/css/style.css` 顶部的 `:root`：

```css
--paper:   #fefdfa;   /* 底色（白纸） */
--ink:     #1c1c1e;   /* 文字（墨色） */
--accent:  oklch(0.55 0.15 250);   /* 强调色（蓝） */
--accent-2:#3b82f6;   /* 强调·亮（图标、行内代码） */
```

下面 `[data-theme="dark"]` 是同款变量的夜间版本，改法相同。

### 改字体

四个 HTML 的 `<head>` 里有 Google Fonts 链接（Fraunces 衬线标题 / Noto Serif SC 中文 / IBM Plex Mono 标签）。换成你喜欢的字体即可。

---

## 8. 常见问题

**Q：双击 HTML 打开是空白？**
A：用 `python3 tools/blog.py serve` 起本地服务，不要用 `file://` 直接打开。

**Q：推上去之后文章没出现？**
A：进仓库 **Actions** 页看是否报错；多半是 frontmatter 写错（比如 `date` 不是 `YYYY-MM-DD`）。`python3 tools/blog.py check` 会自动提示。

**Q：`index.json` 总变化，想完全手动控制？**
A：删掉 `.github/workflows/pages.yml` 里的 `Rebuild` 阶段即可，但此时 `index.json` 需要自己维护（不推荐）。

**Q：想彻底换域名？**
A：改 `tools/blog.py` 的 `SITE_URL`（影响 feed/sitemap），再在 GitHub Pages 设置里配置 Custom domain，并把 `robots.txt` 里的域名同步改掉。

---

## 9. 一句话总结

> **在 `posts/` 里写 `.md`，然后 `python3 tools/blog.py publish "文章标题" --push`。**
> 剩下的，交给 GitHub Actions。